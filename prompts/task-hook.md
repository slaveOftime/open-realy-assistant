tli task hook for {{assistant_name}}:

Use the compact `tli` state below for one bounded supervisor pass.

- Prefer the listed ready/active task information over broad repo scanning.
- If no action is needed, say so briefly.
- If a task needs work, choose one concrete next step and either do it directly if tiny or delegate it intentionally.
- Keep task status in `tli`; do not create side trackers.
- Do not inspect unrelated history, private memory, old schedules, or external notification state.

## tli state

{{tli_state}}

## tli ready

{{tli_ready}}
